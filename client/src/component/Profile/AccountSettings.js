import React from "react";

function AccountSettings({ user, setReload }) {
  console.log(user);
  return (
    <div>
      <input type="password" />
    </div>
  );
}

export default AccountSettings;
